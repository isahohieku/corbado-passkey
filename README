## Project Readme

### Auth Application

A simple application with both frontend and backend to demonstrate auth sessions using Corbado plugins

### Run App

To install dependencies <br />

run `yarn frontend:install` for frontend and
run `yarn api:install` for backend

To run application <br /> 

run `yarn frontend:start` for frontend and
run `yarn api:start` for backend

### Note

The multi sort is only implemented for the Age and email columns